# OpenAI Integration

OpenAI calls must stay server-side. Phase 2 will add:

- Album blueprint generation
- YouTube package drafts
- Switch It Up prompt revisions
- Visual blueprint generation

Malformed output, refusal, schema errors and missing credentials must produce clear user-facing failures.

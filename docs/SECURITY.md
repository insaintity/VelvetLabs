# Security

Secrets must never be exposed to the browser.

Server-only environment variables are listed in `.env.example`.

OAuth credentials must be encrypted. AI-generated filenames, enum values, URLs, metadata and storage paths must be validated before use.
